import ExperienceWorkspaceClient from '@/features/experience/workspace/ExperienceWorkspaceClient';

/** 임시 UI 확인용 라우트 (로그인 가드 없음). 검증 후 삭제한다. */
export default function MapPreviewPage() {
  return <ExperienceWorkspaceClient />;
}
